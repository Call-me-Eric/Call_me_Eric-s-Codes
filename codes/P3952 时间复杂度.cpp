#include <iostream>
#include <cstdio>

using namespace std;

int main(){
	int t;
	scanf("%d", &t);
	while(t--) {
		int l, fix;	char on[10];	//fix为时间复杂度 
		scanf("%d%s", &l, on);
		if(on[2]== '1') fix = 0;		//0表示o(1)
		else if(o[2] =='n')	fix = o[4] - '0'; //o(n^w) 
		for(int i = 1; i <= l; i++){
			scanf("%s", str);
			if(str[0] == 'F')
		}
	}
	return 0;
}

